<%= render partial: 'common/header', locals: {sub_title: '聚会', breadcrumb: '列表', new_path: new_gathering_path} %>

<div class="row">
  <div class="col-lg-12">
    <p>此处的聚会列表只用于欢迎新朋友的表单中，询问新朋友愿意参加我们的哪些聚会。</p>
    <table class="table table-bordered table-hover tablesorter tablesorter-default" role="grid">
      <thead>
      <tr>
        <td> #</td>
        <td> <i class="fa fa-user"></i> 聚会名称 <i class="fa fa-sort"></i></td>
        <td> <i class="fa fa-comment"></i> 备注 <i class="fa fa-sort"></i></td>
        <td> &nbsp; </td>
        <td> &nbsp; </td>
      </tr>
      </thead>
      <tbody>
      <% @gatherings.each do |gathering| %>
          <tr>
            <td style="width:30px;min-width:30px;"> <%= gathering.id %></td>
            <td style="width:250px;min-width:250px;"> <%= gathering.gathering%></td>
            <td style="min-width:30px;"> <span title="<%=gathering.description  %>"><%= truncate(gathering.description, :length => 20) %></span> </td>
            <td style="width:70px;min-width:70px;">
              <%= link_to('<i class="fa fa-edit"></i> 编辑'.html_safe, edit_gathering_path(gathering)) %>
            </td>
            <td style="width:70px;min-width:70px;">
              <%= link_to('<i class="fa fa-trash-o"></i> 删除'.html_safe, gathering_path(gathering), :class => "red", :method => :delete, data: { confirm: '你确定吗？' }) %>
            </td>
          </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div><!-- /.row -->